{
  "name": "react-shop",
  "version": "0.2.0",
  "homepage": "http://krzysu.github.io/reactjs-shopping-cart",
  "private": true,
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "react-scripts": "^5.0.1"  // Actualiza react-scripts si es necesario
  },
  "dependencies": {
    "bootstrap": "^5.1.3",  // Actualiza a una versión más reciente
    "prop-types": "^15.8.1", // Actualiza a una versión más reciente
    "react": "^18.2.0",  // Actualiza a una versión más reciente
    "react-dom": "^18.2.0",  // Actualiza a una versión más reciente
    "react-redux": "^8.0.5",  // Actualiza a una versión más reciente
    "redux": "^4.2.0",  // Actualiza a una versión más reciente
    "node-fetch": "^2.6.7",  // Actualiza a la versión segura
    "ua-parser-js": "^0.7.24"  // Actualiza a la versión segura
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
